name: someInfraCode

on:
  push:
    branches: [ main ]

# Lock down default GITHUB_TOKEN so we don't trigger excessive-permissions
permissions: {}

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      # Single vulnerability: unpinned action (tag instead of commit SHA)
      - name: Set up Node.js (unpinned)
        uses: actions/setup-node@v4   # real action, but only pinned to a tag

      # Safe, non-vulnerable step
      - name: Build
        run: echo "Build step running"